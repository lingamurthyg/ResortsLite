apiVersion: v1
kind: Service
metadata:
  name: resortslite-service
  namespace: resortslite
  labels:
    app: resortslite
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: resortslite
